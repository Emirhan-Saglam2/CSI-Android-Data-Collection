<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- UDP üzerinden CSI paketlerini almak için -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 16 yerel ağ/UDP erişimi için -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Eski USB modu korunuyor ancak zorunlu değil -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Experiments.TimerMainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Eski deney ekranları yalnızca uygulama içinden erişilebilir -->
        <activity
            android:name=".Experiments.PressAndHoldMainActivity"
            android:exported="false" />

        <activity
            android:name=".Experiments.ToggleMainActivity"
            android:exported="false" />

        <activity
            android:name=".Experiments.ManualInputMainActivity"
            android:exported="false" />

        <service
            android:name="com.stevenmhernandez.esp32csiserial.UsbService"
            android:exported="false" />

    </application>

</manifest>